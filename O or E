#include<stdio.h>
int main()
{
    long long n,sum=0,rem;
    scanf("%lld",&n);
    while(n)
    {
        rem=n%10;
        if(rem%2!=0)
        {
            sum=sum+rem;
        }
        n=n/10;
    }
    if(sum%2==0)
    printf("E");
    else
    printf("O");
    return 0;
}
